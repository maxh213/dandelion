# 010 route-command — qa

## Done
- Wrote `qa/010-route-command.e2e.mjs`, which follows qa/010-route-command.md:
  - One node fixture `q` stands in for claude, agy, kimi, codex and kilo (it picks by name) and reads its values from `Q_*` variables.
  - Grok snapshots and cursor auth go into a fresh HOME for each case, and each HOME has an empty `.claude-work`.
  - The cursor API runs in-process on a free port rather than 48010. Kimi also gets a free port for each run.
  - PATH has only the fixture dir plus symlinks to node and sh. HOME and TZ are set, and the only DANDELION_* variables are KIMI_PORT and CURSOR_API_BASE.
  - The test picks TZ `Etc/GMT±(utcHour-11)` and checks that local time lands between 10:00 and 13:59. Reset times are relative to now: +2h is today, +14h is after midnight.
- Covered:
  - All 13 rows of "route prints one line", with exact stdout, empty stderr and exit 0.
  - "Nothing to route": `none\n`, empty stderr, exit 1, with codex and kilo "ok".
  - "route anywhere but first keeps the dashboard" (`--once route`, `routes`, NO_COLOR=1): the first line matches `DANDELION hh:mm:ssZ` and the exit code is 0.
- At the end it removes every temp dir prefixed `dandelion-qa-010-` and checks that no process with that prefix is still running.
- `marestail gate --tier qa`: GATE PASSED (544 tests, qa passed). `git diff ec04f8e -- qa/00*.e2e.mjs` is empty.

## Product defects
- None found. Every row passed on the first run against the current code.

## Left
- Manual QA steps 7 (`dandelion route` on a real terminal) and 10 (dashboard compared with 009 step 2, README grep) are not in the e2e.
  The feature does not require them there. main.test.ts covers the terminal case, and the 009 e2e still passes unchanged.

## Next role must know
- The 010 e2e takes about 11 s. All 16 runs happen one after another, and each run starts all eight probes.
- The QA doc's fixture-served cursor port 48010 was swapped for an in-process server on a free port. Behaviour is the same, and it avoids clashing with a busy port.

## Config change
- None needed. Optional: proposal 23 (the agy `Flash Five Hour Limit` row in the feature outline) is still open for maintainers.
