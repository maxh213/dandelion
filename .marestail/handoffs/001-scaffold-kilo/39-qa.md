# Handoff: QA

## What I did
- Ran the app for real: fixture `kilo` on PATH (colour and `NO_COLOR`), `ALLOWANCE_KILO_REFERENCE=10`, and an empty PATH. All match `qa/001-scaffold-kilo.md`. Exit is 0 every time.
- Rewrote `qa/001-scaffold-kilo.e2e.mjs` to automate procedure steps 1–4:
  - Default reference, colour: banner `ALLOWANCE … HH:MM:SSZ`, heavy `━`×72 rule, `$14.15` followed by 14 `█` and 6 `░`, dim `api balance · kilo` caption, every line ≤ 72 columns.
  - Reference 10 with `NO_COLOR`: gauge is 20 `#`, no escape codes.
  - Empty PATH: `kilo` followed by exactly `kilo CLI not found in PATH`. No `Command failed`, no gauge, no balance, and the panel is dim (`\x1b[90m`).
  - Checks the exit code directly (`spawnSync` status 0). Strips the inherited `NO_COLOR`/`ALLOWANCE_KILO_REFERENCE`. The fixture uses `printf` (not `echo "\n"`), rejects arguments other than `profile`, and lives in `os.tmpdir()`, not `qa/`.
- `qa/e2e.mjs` now exits 1 if the runner itself throws. Before, `.catch(console.error)` exited 0.
- Regression check: I made `src/app/index.ts` stop recognising ENOENT. `node qa/e2e.mjs` failed on the exact-reason assertion and exited 1. Then I restored the file.
- `marestail gate --tier qa` prints GATE PASSED.

## What is left
Nothing for this task.

## Product defects
None found.

## What the next role must know
- This replaces proposal 36. The e2e no longer accepts `Command failed`.
- Your shell may have `NO_COLOR=1` set. The e2e clears it for each run.
- The real `kilo` sits at `~/.local/bin/kilo`. The e2e puts the fixture first on PATH, or uses a PATH with only an empty temp dir, so the real CLI is never reached.
- Keep vitest pinned to 4.1.11 (see 35-coder).

## Config change
The Stop hook `marestail gate --hook` still fails `ts.deps` on a clean tree. Its only "findings" are depcruise's `✔ no dependency violations found` and npm notices, and `npx depcruise --config .dependency-cruiser.cjs --output-type err src` exits 0. `marestail/gates/ts_deps.py::scoped_findings` should return `[]` when no line matches `VIOLATION`. `--tier qa` passes `ts.deps`.

If the runner treats `qa/*.e2e.mjs` as frozen for QA too, re-apply this commit: it is the QA harness this role is meant to own. The procedure `.md` is unchanged.
