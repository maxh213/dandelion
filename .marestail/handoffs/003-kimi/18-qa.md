# 003-kimi — qa

## Done
- `qa/003-kimi.e2e.mjs` (written by 16-specifier) already matches the task's QA procedure and "Earlier end-to-end checks keep passing". It checks: a free port, the `allowance-qa-003-` prefix, a 60s outer timeout, the ok run at 59%/42% between agy and kilo, the no-token run with exit 0 in under 30s and the unavailable card, the pid from the pid file gone after each run, and no `allowance-qa` process left. No harness change was needed, so none was made.
- `node qa/e2e.mjs`: PASS for 001, 002, 002-readme and 003 (4.0s).
- `marestail gate --tier qa`: GATE PASSED (226 tests, 100% coverage, CRAP ≤ 4, qa passed).
- I ran the manual procedure in qa/003-kimi.md against the real app (`npm start`, NO_COLOR, fixture from the doc, restricted PATH). Every run exited 0, the child was GONE, no line was over 72 columns, and no NaN or Infinity appeared:
  - ok: `59% ↻ 5d4h` / `42%` (see below); odd: `120%` full gauge, no ↻ / `70%`
  - zero, badjson: `Could not parse usage from response`; 500: `kimi usage request failed: HTTP 500` (1s)
  - notoken: `kimi web exited without printing a token` (1s); nohttp: `kimi usage request failed` (1s)
  - hang: `kimi usage request timed out after 10s` (11s); silent: `kimi web printed no token within 20s` (21s)
  - stubborn: ok rows, 6s; ALLOWANCE_KIMI_PORT abc/0/70000: `ALLOWANCE_KIMI_PORT must be an integer from 1 to 65535`
- No `/tmp/allowance-kimi-*` log dir was left behind by any mode, by a missing kimi, or by the e2e run (dir count before and after each run was the same).

## Product defects
- None found.

## Left
- qa/003-kimi.md step 2 expects `↻ 5d0h`, but its fixture sets `reset_at` 7500 min ahead, so a tester sees `↻ 5d4h`. This is a doc error, not a product defect. I confirmed it live.

## Config change
- qa/003-kimi.md:24: change `7500 * 60000` to `7205 * 60000`. The setup text already says "5 days and 5 minutes", step 2 then holds, and the e2e already uses 7205. A human must apply it, because the runner reverts edits to the QA procedure.

## What the next role must know
- /tmp holds about 148 old `allowance-kimi-*` dirs dated before this run, probably from mutation or gate runs by earlier roles. The app itself leaks none. They are safe to delete by hand.
- The e2e's leftover-process scan reads Linux `/proc`.
